spring.config.import=optional:classpath:.env
spring.application.name=${APP_NAME:backend}
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/flores_eternas_lp}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
jwt.secret=${JWT_SECRET}
frontend.url=${FRONTEND_URL:}
spring.mail.host=${SPRING_MAIL_HOST:${spring.mail.host:smtp.gmail.com}}
spring.mail.port=${SPRING_MAIL_PORT:${spring.mail.port:587}}
spring.mail.username=${SPRING_MAIL_USERNAME:${spring.mail.username:}}
spring.mail.password=${SPRING_MAIL_PASSWORD:${spring.mail.password:}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
server.port=${PORT:8080}
